# Copy this file to .env and edit it before running the stack.

# Local Minecraft server
MC_PORT=43027
MC_LOCAL_HOST=127.0.0.1
MC_MIN_RAM=1G
MC_MAX_RAM=3G
SERVER_JAR_PATTERN=purpur-*.jar

# Public entry probes shown in the panel.
# First host = main relay, second/extra hosts = optional acceleration entries.
# Example: play.yourdomain.com,acceleration.yourdomain.com
# UNMC currently uses: playje.unmcserver.com,la.playje.unmcserver.com
MC_ENTRY_HOSTS=play.yourdomain.com,acceleration.yourdomain.com

# SSH reverse tunnel from the Mac mini to the main relay server.
REMOTE_HOST=relay.example.com
REMOTE_USER=minecraft_tunnel
REMOTE_SSH_PORT=22
REMOTE_BIND=0.0.0.0
REMOTE_FORWARD_PORT=43027
LOCAL_HOST=127.0.0.1
LOCAL_PORT=43027
SSH_KEY_PATH=${HOME}/.ssh/unmc_tunnel_ed25519
SSH_KNOWN_HOSTS_FILE=${HOME}/.ssh/known_hosts

# Local web panel. Keep this on localhost unless you add authentication.
PANEL_HOST=127.0.0.1
PANEL_PORT=8765
PANEL_URL=http://127.0.0.1:8765

# Screen session names.
SCREEN_PREFIX=unmc
SERVER_SCREEN_NAME=unmc-je
TUNNEL_SCREEN_NAME=unmc-tunnel
PANEL_SCREEN_NAME=unmc-panel
